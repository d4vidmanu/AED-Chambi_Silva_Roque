#include "ForwardList.h"

int main() {
    // Crear una instancia de ForwardList para probarla
    ForwardList<int> list;

    // Pruebas de las funciones miembro de la clase ForwardList
    list.push_back(10);
    list.push_back(20);
    list.push_back(30);

    // Aquí puedes agregar más pruebas según vayas implementando las funciones

    return 0;
}